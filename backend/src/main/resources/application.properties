spring.application.name=analisador-fipe-api

# --- BANCO DE DADOS (COMENTADO PARA TESTES) ---
# spring.datasource.url=jdbc:h2:mem:fipe_db
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- CONSOLE H2 ---
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# --- HIBERNATE ---
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# --- CHAVE API FIPE (ATIVO) ---
fipe.api.token=${FIPE_TOKEN:insira_seu_token_aqui}

# Desativa a inicialização do banco de dados
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

server.port=8081